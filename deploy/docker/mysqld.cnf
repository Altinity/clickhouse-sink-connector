[mysqld]
max_connections=100000


gtid-mode = on
enforce-gtid-consistency = true
# for loading files
local_infile = on
# to support tables without PK
sql_generate_invisible_primary_key=1
# perf related, commented out
#innodb_buffer_pool_size=8G
#innodb_log_file_size=1G
